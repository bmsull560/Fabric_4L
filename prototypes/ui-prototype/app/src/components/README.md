# Prototype components namespace

Prototype-only UI primitives were moved to:

- `_ui-prototype/non-production/ui-components-archive`

This keeps non-production assets out of paths that resemble active product component trees.
