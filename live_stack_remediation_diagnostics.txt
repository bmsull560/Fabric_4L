# Live stack remediation diagnostics

Generated by Manus AI during live workflow validation for `bmsull560/Fabric_4L`.

## Scope

This evidence records the final permitted Remediation Backlog Item 1 attempt: make the backend-integrated live stack start so Playwright live UI validation could eventually proceed. No mocked E2E, fixture-only route, MSW response, component-only check, or traceability result was counted as live success.

## Targeted repair attempt

| Step | Result |
|---|---|
| Docker socket access | `sudo chmod 666 /var/run/docker.sock` made `/var/run/docker.sock` accessible to the `ubuntu` user for this sandbox session. |
| Legacy compose dependency compatibility | `requests` was pinned from `2.33.1` to `2.28.2`; `urllib3` was pinned from `2.6.3` to `1.26.20`; `docker==5.0.3` was installed. |
| Docker daemon query | `docker info --format 'Server={{.ServerVersion}} Driver={{.Driver}} Cgroup={{.CgroupDriver}}'` returned `Server=29.1.3 Driver=overlayfs Cgroup=systemd`. |
| Compose query | `docker-compose version` returned v1.29.2 and `docker-compose ps` returned service state rather than the earlier `URLSchemeUnknown: Not supported URL scheme http+docker` exception. |
| Further Docker repair | Stopped. This was the final allowed targeted Docker repair cycle. |

## Post-repair service state

| Container | State |
|---|---|
| `vf-bi-postgres` | `Up (healthy)` on `5432` |
| `vf-bi-redis` | `Up (healthy)` on `6379` |
| `vf-bi-neo4j` | `Up (healthy)` on `7474` and `7687` |
| `vf-bi-minio` | `Up (healthy)` on `9000` and `9001` |
| `vf-bi-layer1` | `Up (unhealthy)` on `8001` |
| `vf-bi-layer1-worker` | `Up (unhealthy)` |
| `vf-bi-layer2` | `Exited (1)` |
| `vf-bi-layer3` | `Exited (1)` |
| `vf-bi-layer4` | `Created` |
| `vf-bi-layer5` | `Created` |
| `vf-bi-layer5-migrate` | `Exited (1)` |
| `vf-bi-layer6` | `Exited (1)` |
| `vf-bi-minio-init` | `Exited (0)` |

## Endpoint probes

| Port | Expected layer | Probe result |
|---:|---|---|
| `8001` | L1 ingestion | `/health`, `/healthz`, and `/ready` timed out with `http_code=000`, curl exit `28`. |
| `8002` | L2 extraction | `/health`, `/healthz`, and `/ready` returned connection refused with curl exit `7`. |
| `8003` | L3 knowledge | `/health`, `/healthz`, and `/ready` returned connection refused with curl exit `7`. |
| `8004` | L4 agents | `/health`, `/healthz`, and `/ready` returned connection refused with curl exit `7`. |
| `8005` | L5 ground truth | `/api/v1/health`, `/health`, and `/ready` returned connection refused with curl exit `7`. |
| `8006` | L6 benchmarks | `/health`, `/healthz`, and `/ready` returned connection refused with curl exit `7`. |

## Service-level blocker details

| Service | Exact blocker observed in logs |
|---|---|
| L1 ingestion | API process starts but container is unhealthy; worker repeatedly reports `Cannot connect to redis://redis:6379/0: Timeout connecting to server`. |
| L2 extraction | Uvicorn import fails with `IndexError: 4` at `services/layer2-extraction/src/layer2_extraction/shared_bootstrap.py`, where `_SHARED_SRC` uses `Path(__file__).resolve().parents[4]`. |
| L3 knowledge | Uvicorn import fails with `ImportError: cannot import name 'get_system_metrics' from 'value_fabric.layer3.api.app_monolith'`. |
| L4 agents | Service remains `Created` because compose dependencies require healthy L1, L2, L3, L5, and L6 services. |
| L5 ground truth | `alembic upgrade head` exits with `psycopg2.OperationalError` because connection to `postgres:5432` times out. |
| L6 benchmarks | Uvicorn import fails with `IndexError: 3` at `services/layer6-benchmarks/src/shared_bootstrap.py`, where `_SHARED_SRC` uses `Path(__file__).resolve().parents[3]`. |

## Classification

The Docker/compose access blocker was remediated, but the live stack still fails the Live Environment Gate because L1–L6 do not all start and return healthy host-accessible responses. Under the user-specified rules, live workflow validation remains **NO GO** and all 30 workflow areas remain **BLOCKED**.

No secrets are intentionally recorded in this diagnostics file.
