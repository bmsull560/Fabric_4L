# Temporary skip allowlist for pytest collection governance.
# Every entry requires owner + expiry (YYYY-MM-DD).
entries:
  - pattern: "pending refactor: legacy auth fixture"
    owner: "@value-fabric/backend-leads"
    expires: "2026-06-30"
    ticket: "VF-0000"
