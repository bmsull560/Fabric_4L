# Frontend Legacy Root Policy

`apps/web/` is the canonical and only active frontend source root.

The legacy `frontend/` tree is retained only for documentation and archival markdown content.
Do not add runtime source files under `frontend/`.

If you need active frontend code, place it under `apps/web/`.
