# Layer 6 Drift Audit — <milestone-id>

## Endpoint inventory

- List canonical Layer 6 endpoints reviewed.
- Note wrapper modules and canonical runtime modules.

## Frontend usage mapping

- Map endpoint consumers in `apps/web/` (or state "none").
- Note TanStack Query hooks / API client touchpoints.

## Gateway exposure matrix

- Record whether each endpoint is exposed through gateway routes.
- Call out auth, tenant context, and RBAC expectations.

## Risk classification

- Classify each finding (`low`, `medium`, `high`).
- Include drift type (contract, auth, tenant, wrapper divergence).

## Owner sign-off block

- **Owner:**
- **Reviewer:**
- **Sign-off date (UTC):**
- **Decision:** (`approved`, `follow-up required`)
