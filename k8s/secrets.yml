apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: value-fabric
  labels:
    value-fabric.io/environment: dev
  annotations:
    value-fabric.io/warning: "For local development only. Do not use in production."
type: Opaque
stringData:
  password: "VF-dev-db-P@55-rotate-before-shared-use"  # Local dev example; rotate before use
---
apiVersion: v1
kind: Secret
metadata:
  name: neo4j-secret
  namespace: value-fabric
  labels:
    value-fabric.io/environment: dev
  annotations:
    value-fabric.io/warning: "For local development only. Do not use in production."
type: Opaque
stringData:
  auth: "neo4j/VF-dev-graph-P@55-rotate-before-shared-use"  # Local dev example; rotate before use
---
apiVersion: v1
kind: Secret
metadata:
  name: openai-secret
  namespace: value-fabric
  labels:
    value-fabric.io/environment: dev
  annotations:
    value-fabric.io/warning: "For local development only. Do not use in production."
type: Opaque
stringData:
  api-key: "vf-dev-openai-key-replace-via-secret-manager"  # Local dev example; set via secret manager when possible
---
apiVersion: v1
kind: Secret
metadata:
  name: jwt-secret
  namespace: value-fabric
  labels:
    value-fabric.io/environment: dev
  annotations:
    value-fabric.io/warning: "For local development only. Do not use in production."
type: Opaque
stringData:
  secret: "VF-dev-jwt-secret-32chars-minimum-rotate-now"  # Local dev example; rotate before use
