apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: value-fabric-prod

resources:
  - layer4-agents-rollout.yaml
  - layer1-ingestion-rollout.yaml

commonLabels:
  app.kubernetes.io/part-of: value-fabric
  app.kubernetes.io/managed-by: argo-rollouts
  rollout: enabled
