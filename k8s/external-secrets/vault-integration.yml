# HashiCorp Vault Integration for Value Fabric Secrets
# 
# This manifest provides an example of using Vault for production secret management.
# For local development, use k8s/secrets.yml with placeholder values.
# For production, integrate with your Vault instance.
#
# Prerequisites:
# - Vault cluster running and accessible from K8s cluster
# - Kubernetes auth method enabled in Vault
# - External Secrets Operator installed in cluster

---
# External Secret: OpenAI API Key
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: openai-api-key
  namespace: value-fabric
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: openai-secret
    creationPolicy: Owner
  data:
    - secretKey: api-key
      remoteRef:
        key: value-fabric/llm
        property: openai-api-key

---
# External Secret: Neo4j Credentials
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: neo4j-credentials
  namespace: value-fabric
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: neo4j-secret
    creationPolicy: Owner
  data:
    - secretKey: auth
      remoteRef:
        key: value-fabric/database
        property: neo4j-auth

---
# External Secret: JWT Secret
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: jwt-signing-secret
  namespace: value-fabric
spec:
  refreshInterval: 24h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: jwt-secret
    creationPolicy: Owner
  data:
    - secretKey: secret
      remoteRef:
        key: value-fabric/auth
        property: jwt-secret

---
# External Secret: PostgreSQL Password
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: postgres-password
  namespace: value-fabric
spec:
  refreshInterval: 24h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: postgres-secret
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: value-fabric/database
        property: postgres-password

---
# ClusterSecretStore: Vault Backend Configuration
# This must be adapted to your specific Vault setup
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: "https://vault.example.com:8200"  # Change to your Vault URL
      path: "secret"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "value-fabric-role"
          serviceAccountRef:
            name: "external-secrets-sa"
            namespace: "external-secrets"
