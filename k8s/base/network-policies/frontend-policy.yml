apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: frontend-policy
  namespace: value-fabric
spec:
  podSelector:
    matchLabels:
      app: frontend
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Allow from ingress controllers (adjust namespace/labels as needed)
    - from:
        - namespaceSelector:
            matchLabels:
              app.kubernetes.io/name: ingress-nginx
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
        - namespaceSelector: {}  # Fallback: allow from any namespace (common ingress controllers)
      ports:
        - protocol: TCP
          port: 3000
  egress:
    # Allow to L4 agents
    - to:
        - podSelector:
            matchLabels:
              app: layer4-agents
      ports:
        - protocol: TCP
          port: 8000
    # Allow to L5 ground truth
    - to:
        - podSelector:
            matchLabels:
              app: layer5-ground-truth
      ports:
        - protocol: TCP
          port: 8005
    # Allow to L6 benchmarks
    - to:
        - podSelector:
            matchLabels:
              app: layer6-benchmarks
      ports:
        - protocol: TCP
          port: 8006
    # Allow DNS
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: UDP
          port: 53
